#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - generate random valid password
 *
 * Return: Always 0
 *
 */
int main(void)
{
	int pass{
		[100];
		int i, sum, n;

		sum = 0

			srand(time(NULL));
		for 
